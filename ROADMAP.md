# Roadmap · 项目路线图

> 本文档描述 Question Notebook 的长期发展方向与版本规划。
> 更新频率：随版本迭代同步更新。

---

## 项目愿景

从**个人问题记录工具**出发，逐步演进为**问题驱动的知识管理与社区协作平台**：

- 个人层面：系统化记录、追踪、沉淀自己遇到的问题
- 社区层面：用户发布问题、互相回答（问答社区）
- 连接层面：自动采集社会问题，分析来源与脉络，将个人问题与社会议题关联起来

## 当前状态

**v0.2.1（2026-08-22）** ✅ 数据可视化已完成

在 SQLite 存储基础上，数据层新增 `get_stats()` 用 SQL 聚合算出分类分布、解决率、按月趋势；Web 端新增 `/api/stats` 接口与可折叠图表面板（原生 Canvas 绘制分类分布柱状图 + 解决率环形图，零第三方依赖，随数据实时刷新）。v0.2.x 仅剩局域网部署一项。

## 版本规划

### v0.2.x · 数据升级（本地，进行中）

**目标**：支撑更大数据量，奠定数据分析基础。

| 项目 | 说明 | 状态 |
|------|------|------|
| SQLite 存储迁移 | 替换 JSON 文件存储，`models.py` 内部换实现，界面层零改动 | ✅ 已完成 |
| 数据可视化 | 分类分布柱状图、解决率环形图（Web 端，原生 Canvas），后端 SQL 聚合 | ✅ 已完成 |
| 局域网部署 | 支持内网多设备访问（手机/平板） | 📋 规划中 |

**验收标准**：万级数据量下操作无卡顿；可视化图表正确反映数据分布。

### v0.3.0 · 社区化（服务器）

**目标**：从个人工具变为多人使用的问答平台。

| 项目 | 说明 |
|------|------|
| 用户系统 | 注册/登录、密码加密存储、会话管理 |
| 问答模型 | 问题 1:N 回答、采纳最佳答案、点赞 |
| 数据隔离 | 用户数据相互隔离，权限控制 |
| 云部署 | 云服务器 + 域名 + HTTPS |

**验收标准**：两个以上用户可同时使用，数据互不可见；公网可访问。

### v0.4.0 · 社会问题采集（数据接入）

**目标**：引入外部数据源，连接个人与社会。

| 项目 | 说明 |
|------|------|
| 采集器 | 定时抓取社会问题数据源（热榜/舆情/政务留言板） |
| 数据存储 | 采集数据入 SQLite，支持增量更新 |
| 基础分析 | 分词、关键词提取、热度趋势统计 |

**验收标准**：至少两个数据源稳定采集一周；采集数据可查询、可统计。

### v1.0.0 · 关联与发布（平台化）

**目标**：形成完整产品闭环，对外发布。

| 项目 | 说明 |
|------|------|
| 关联引擎 | 个人问题 ↔ 社会问题自动匹配（关键词/相似度） |
| 脉络分析 | 问题来源追踪、时间线演化展示 |
| 产品化 | 稳定性、备份策略、运营后台 |

**验收标准**：个人问题可关联到相关社会议题并展示关联理由；系统可持续稳定运行。

## 非目标（Non-Goals）

明确**不做**的事，防止范围蔓延：

- ❌ 不做移动端原生 App（Web 端优先，后续视需求评估）
- ❌ 不做实时聊天/IM 功能（问答为异步模式）
- ❌ 不依赖付费 AI 大模型 API（分析功能以经典算法实现为主，成本可控）
- ❌ 不做多语言国际化（中文优先，验证需求后再考虑）

## 变更记录

| 日期 | 变更 |
|------|------|
| 2026-08-22 | v0.2.1 完成数据可视化（get_stats + /api/stats + Web Canvas 图表） |
| 2026-08-22 | v0.2.0 完成 SQLite 存储迁移（schema.sql + migrate_to_sqlite.py + models.py 改造） |
| 2026-08-15 | 创建路线图，确立 v0.1.0 ~ v1.0.0 版本规划与项目愿景 |
| 2026-08-16 | v0.1.2 完成安全加固（认证 + CSRF + 跨进程锁），从 v0.2.0 移除对应前置项 |

---

> 相关文档：[README.md](README.md)（项目说明）· [TUTORIAL.md](TUTORIAL.md)（教学文档）
